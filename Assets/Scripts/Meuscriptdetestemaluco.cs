using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Meuscriptdetestemaluco : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        new GameObject();
        //Comentario inutil
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("gussssssssss");
    }
}
